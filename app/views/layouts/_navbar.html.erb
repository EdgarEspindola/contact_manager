<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-normal text-uppercase navbar-brand">
    <%= link_to "My contact", root_path %>
  </h5>
  <nav class="mr-auto">
    <%= link_to "Dashboard", dashboard_path, class: "#{active_item('dashboard')} p-2 text-dark btn btn-light" %>
    <%= link_to "All Contacts", contacts_path, class: "#{active_item('contacts')} p-2 text-dark btn btn-light" %>
  </nav>

  <% if user_signed_in? %>

  <%= render 'layouts/search_form' %>

  <nav class="my-2 my-md-0 mr-md-3">
    <div class="dropdown">
      <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="far fa-user"></i>
        <%= current_user.name %>
      </a>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <%= link_to "Profile", edit_user_registration_path, class: "dropdown-item" %>
        <%= link_to "Logout", destroy_user_session_path, class: "dropdown-item", method: :delete %>
      </div>
    </div>
  </nav>
  <% else %>
  <nav class="">
    <%= link_to "Login", new_user_session_path, class: "p-2 text-dark btn btn-light" %>
    <%= link_to "Register", new_user_registration_path, class: "p-2 text-dark btn btn-primary" %>
  </nav>
  <% end %>

</div>
